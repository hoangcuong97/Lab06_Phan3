@model IEnumerable<QL_TrungTam_X.Areas.QuanLy.Models.Certificate>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CertificateName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReleaseDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExpirationDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Coure.CouresName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CertificateName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReleaseDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExpirationDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Coure.CouresName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CertificateID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CertificateID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CertificateID })
        </td>
    </tr>
}

</table>
